---
name: archive-organizer
description: 通用文件归档/目录整理套件。当用户要求归档、整理、分类文件，建档案目录骨架，压缩/扁平化嵌套目录，合并散乱文件夹，批量移动文件，增量整理新增文件，重排目录骨架/换分类体系（逐文件映射回放），检测语义错位文档，或说"这堆文件太乱了帮我整理"时使用——即使没说"归档"二字也应触发。十一个脚本：全量扫描、骨架评审CSV、深度内容提取、规则体检、规则化移动（默认干跑+日志回滚）、骨架重排映射生成、映射回放执行、人工调整自学习（从移位样本提炼学习规则）、语义错位探测、位置快照（人工调整保护+增量检测）、归档目录隐藏锁定（win/mac）。所有项目关键词与画像只存在于各项目的规则CSV和目录自身，技能本体不含任何特定项目的词。
---

# archive-organizer · 通用归档套件

把「散乱目录」整理成「扁平 + 标签化」的档案结构。核心理念：**文件夹只划层级，文件靠标签压平；不增层**。任何脚本都不写死路径——归档根一律用 `--root` 传入，跨项目直接复用。

## 安全铁律（每次都要遵守）

1. **先扫描后动手**：第一步永远先跑 `ao_scan.py`，只读不写。
2. **默认干跑**：`ao_classify.py` 不加 `--execute` 永远只预览。真实移动必须先给用户看过预览并确认。
3. **人工评审是必经环节**：骨架 CSV 和规则 CSV 都要给用户确认后才执行。
4. **只移动不删除**：套件没有任何删除文件的能力；撤销靠 `--undo` 回放移动日志。
5. **深度提取是最后手段**：文件名能判断的绝不读正文（读正文慢且可能读不到）。

## 产物自包含（重要约定）

所有产物默认生成在**待整理文件夹内部**的 `归档整理/` 子目录：

```
<root>/归档整理/
├── 清单.txt            ← ao_scan 产出
├── 骨架评审.csv        ← ao_schema 产出（给人审）
├── 深度提取报告.txt    ← ao_deep 产出
├── 归档规则.csv        ← 人审结论翻译成的执行规则（ao_classify 读）
└── .ao_journal.jsonl   ← 移动日志（ao_classify 写，--undo 用）
```

扫描、深挖、移动遍历都会**自动跳过** `归档整理/`，不会把整理产物当档案。参数 `--out`/`--rules`/`--journal` 可改位置，但默认不要改——自包含让整个整理过程随文件夹走。

## 十个脚本（位于本 skill 的 scripts/ 下）

| 脚本 | 作用 | 典型调用 |
|---|---|---|
| `ao_scan.py` | 全量扫描生成清单（只读） | `ao_scan.py --root <项目根>` |
| `ao_schema.py` | 清单 → 骨架评审CSV（不移动） | `ao_schema.py --root <项目根>` |
| `ao_deep.py` | 读文档正文辅助定类（只读） | `ao_deep.py --root <根> --domain <某域> --limit 10` |
| `ao_semantic.py` | 语义错位探测：域画像推导+候选CSV（只读） | `ao_semantic.py --root <根>` |
| `ao_verify.py` | 静态核验规则CSV与目录现状（只读） | `ao_verify.py --root <根>` |
| `ao_remap.py` | 骨架重排映射生成：移树+归槽规则CSV → 逐文件映射总表（只读） | `ao_remap.py --root <根> [--detail]` |
| `ao_replay.py` | 逐文件映射总表回放执行（默认干跑，日志兼容 `--undo`） | `ao_replay.py --root <根> --map <映射总表.csv> [--execute]` |
| `ao_state.py` | 位置快照：人工调整保护+增量检测 | `ao_state.py --root <根> snapshot\|diff` |
| `ao_protect.py` | 归档整理目录隐藏+锁定（win/mac） | `ao_protect.py --root <根> lock` |
| `ao_learn.py` | 自学习：从人工调整移位样本提炼学习规则CSV（只读，宁可不学） | `ao_learn.py --root <根>` |
| `ao_classify.py` | 按规则CSV执行移动（默认干跑，--rules 可多份合并） | `ao_classify.py --root <根>` |

## 工作流

**首轮**：scan → schema → 人审CSV → 拟归档规则.csv → verify → 干跑 → 用户确认 → execute → （execute 自动刷新位置基线）→ `ao_protect.py lock`。

**日常增量（含自学习闭环）**（新文件/新文件夹不断涌入的场景）：`ao_protect.py unlock`（各脚本其实会自动解锁，此步可省）→ `ao_state.py diff --apply-protection`（人工调整自动进保护清单，新增文件列增量清单）→ `ao_learn.py`（把本轮人工调整提炼成 `学习规则.csv`：目录整编规则（仅当源目录被整体搬空）+ 当前树未发现误伤的关键词规则，人审删留）→ 对新增文件拟增量规则 → verify → 干跑 → 确认 → `ao_classify --rules 归档规则.csv --rules 学习规则.csv --execute` → lock。**已有文件的人工调整永不被规则回改**——凡在两次基线之间被移动/改名的文件都进保护清单，规则与体检一律跳过；而它们的「去向」经 ao_learn 提炼后反过来指导新文件的归档，形成整理系统自学习闭环。

**语义错位探测**（文件内容在 A 域、语义属 B 域的场景）：`ao_semantic.py` 从目录树自身推导各域用词画像（词的区分度=域内频次÷全域频次，各域通用词自动趋零），输出 `错位候选.csv` 供人工评审。同一词在不同域含义不同（如"报价"之于招投标与物资）由画像分布自然区分——画像来自项目自己的数据，不含任何硬编码词。确认后照常翻译成规则执行。

## 骨架评审 CSV（ao_schema.py 产出，给人审）

六列：`一级目录 | 第二级业务类型（推荐） | 第三级通用文件归档标准 | 是否调整 | 建议方案 | 退回现状`。
- 「否」行 = 锚定行（结构已合理，不动）；「是」行 = 待调整项。
- 域级「建议方案」附带**聚类提示**（如 `合同(10个)、结算(4个)`），即建议的合并主题。
- 阈值可调：`--max-calm`（二级≤N视为已合理，默认12）、`--busy`（文件数≥N建议压平，默认30）、`--top`（每域明细行上限，默认15）。

## 规则 CSV（ao_classify.py 的输入，把人审结论翻译成可执行规则）

写入 `<root>/归档整理/归档规则.csv`。三列，表头 `动作,源,目标`，路径相对 `--root`（ao_classify 的 `--rules` 可多次传入按顺序合并，如增量时同时给 归档规则.csv 与 学习规则.csv）：

```csv
动作,源,目标
并目录,<某域>/01.旧版文件,<某域>/历史版本
移目录,<另一域>/零散报告,<另一域>/报告汇编/2024年度
移文件,<文件名中的独特词组>,<某域>/<主题目录>
```

- `并目录` = 收口压平：源树所有文件直接并入目标目录（不保留子层级）
- `移目录` = 整体搬家：源目录搬到目标下，保留内部结构
- `移文件` = 按名捞取：全根文件名含关键词的文件移入目标
- 重名自动加 ` (1)` 序号；干跑会列出被多条规则命中的冲突文件（按规则顺序先到先得，要改结果请调整规则表顺序）；日志按批次号记录：`--undo` 整体回滚、`--undo-last` 只回滚最近一批、`--undo-batch <ID>` 回滚指定批、`--list-batches` 查看批次列表
- 规则顺序即裁决顺序：脚本先展开全部规则、再按 CSV 顺序执行，先到先得。要让目录规则保留某批文件的内部结构，就把它排在会捞走这些文件的移文件规则之前

## 骨架重排 / 迁移对齐（映射回放式工作流）

适用于：换分类体系（旧骨架 → 新骨架）、跨机对齐、任何「关键词规则表达不了、必须逐文件定路径」的重排。机制在技能（`ao_remap.py` + `ao_replay.py`），数据在项目（`骨架重排规则.csv` + `映射总表.csv`）：

1. **设计稿人审**：新骨架（域划分、层级语法、层级数上限）先给用户定稿。
2. **写骨架重排规则.csv**（纯数据，关键词只在这里）：`移树`（子树整树移植，最长前缀优先）+ `归槽`（散件按名归槽，作用域最长优先，目标自动拼文件名）+ `留守`（垃圾跳过）。
3. `ao_remap.py` 生成映射总表：**未决必须为 0**；「归槽审计」按目录聚合列出所有走散件分派的目录——裸奔目录=漏建移树规则，逐个补；「归槽明细」逐条人工过目。
4. `ao_replay.py` 干跑预检 → 用户确认 → `--execute`（日志兼容 `ao_classify --undo`；执行自动刷新位置基线，与人工调整保护/增量归档机制完全互通——映射含人工保护文件时预检知悉提示，锁定态自动解锁/回锁）。
5. 终验：文件总数守恒、流量矩阵与设计稿对账。

**设计原则（层级语法模板，层数按需约定）**：L1 业务域 / L2 业务块 / L3 对象·子题 / L4 文档类型 / L5 批次·标段·期数·年份；文件落在语义需要的最深槽位，不为凑层造空层；已有语义子树整树移植，只对散件逐文件判槽。

## 规则关键词卫生（通用，适用任何项目）

`移文件` 是全根捞取，关键词质量决定成败。写词与执行纪律（`ao_verify.py` 会自动检查前四条）：

1. **关键词只进项目规则 CSV**——本 skill 与其文档不收录任何具体项目的词；换一个目录，规则从零按该目录现状重拟。
2. **单域命中**：一个关键词的命中文件跨多个一级域，多半是误捞；只有「有意归拢」（如把散在各域的同类台账收拢）才允许跨域，且要在人审时明说。
3. **跨域同名即镜像信号**：同一文件名出现在两个域的平行目录树下，通常是整树镜像副本——先问用户以哪份为准，**不对镜像对写规则**。
4. **拒绝宽泛词**：过短（<3字）或命中过多的词一律拆细，用文件名里独特的词组串。
5. **计数闭环**：verify 的预计移动数 = 干跑计数才允许 execute；执行后如有「跳过已消失」，逐条对账（通常是规则间重叠被先到者裁决）。
6. **归槽词同纪律**：`ao_remap` 的归槽关键词与移文件关键词同规——只进项目规则 CSV、拒绝跨作用域误捞；匹配前工具自动做文件名去空白归一（"承 诺 函"≈"承诺函"），但语义近似词（如"租赁"与"租金"）仍需人审。
7. **兜底行放最后**：每个归槽作用域的空关键词兜底行必须是该作用域最后一行；兜底只是安全网，能写明确词的都写明确词。

## 深度提取的时机

只有当二级目录名完全看不出文件性质（如「03.杂件」「新建文件夹」），且用户要求判断时才用 `ao_deep.py`。它读 docx/xlsx/pptx/pdf/txt 的正文前 N 字（`--head 600`），pdf 依次尝试 pdftotext 命令 → pypdf 库。输出是文本报告（`--out`），供下一轮人工/模型定类，不直接改结构。

## 方法论与算法说明

执行前若对「为什么不建伪二级」「聚类提示怎么来的」有疑问，读 `references/method.md`。里面有：扁平优先与标签压平的完整规则、三层判断逻辑、关键词聚类算法、开源借鉴（organize 的 journal+undo、rclone 的重名序号、rsync 的 exclude）。
